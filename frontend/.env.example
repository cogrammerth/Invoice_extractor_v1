# API base URL (backend Express server)
# Local dev: http://localhost:3000 (or use Vite proxy in vite.config.ts)
VITE_API_URL=http://localhost:3000

# Railway invoice-ui (runtime, written to dist/runtime-config.json on start):
# API_URL=https://invoice-api-production-xxxx.up.railway.app

VITE_APP_NAME=Thai Invoice Extractor
